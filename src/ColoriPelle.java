/**
 * Colori della pelle disponibili per una {@link Persona}.
 */
public enum ColoriPelle {
    /** Pelle bianca. */
    BIANCO,
    /** Pelle mulatta. */
    MULATTO,
    /** Pelle nera. */
    NERO
}